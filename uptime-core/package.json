{
  "name": "uptime-core",
  "private": true,
  "version": "1.0.0",
  "description": "The core library for the uptime monitoring application.",
  "author": "Khalid Zoabi <kzoabi@outlook.com>",
  "license": "MIT",
  "main": "./dist/index.js",
  "files": [
    "dist/**/*",
    "!dist/**/*.test.js"
  ],
  "scripts": {
    "dev": "ts-node ./src/index.ts",
    "clean": "rimraf ./dist/",
    "build": "npm run clean && tsc",
    "test": "npm run build && ava --verbose"
  },
  "devDependencies": {
    "@types/deep-equal": "^1.0.1",
    "@types/uuid": "^3.4.5",
    "ava": "^2.2.0",
    "deep-equal": "^1.0.1",
    "rimraf": "^2.6.3",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "aws-sdk": "^2.503.0",
    "uuid": "^3.3.2"
  }
}
